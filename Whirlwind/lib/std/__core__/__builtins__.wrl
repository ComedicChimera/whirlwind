type __str__ : Iterable<char> {
	let chars: list[char];

	constructor(chars: list[char]) {
		this.chars = chars;
	}

	priv func at(ndx: int) char => this.chars[ndx];

	func begin() uint => 0;
	func len() uint => this.chars.len();
}