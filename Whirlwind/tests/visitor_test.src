struct Test {
	a, b: int;
}

interf for Test {
	operator+ (x: int) {
		this.a += x;
		this.b += x;
	}

	operator[] <T>(ndx: T) T {
		let t = ndx;

		return this.a * t;
	}
}

func main() {
	let t = new Test();

	t += 4;

	let res = t[4.5];

	res = t[5];
}