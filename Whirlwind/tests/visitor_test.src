type Number
	| Int(int)
	| Float(float)
	;

func test(x: Number) {
	x = Int(1);
}

func main() {
	let num = Number::Int(3);

	let value = from (num as Int);

	let boolean = (true as int);

	// fix this boi right here
	value = from (num as Number::Int);

	num = Number::Float(3.14);
}