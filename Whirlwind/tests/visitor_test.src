func main() {
	let (a, b, c): int;

	a += 4;

	b %= 6;

	c += 0.5; // FAIL

	b -= 17;

	b *= 5;

	b ~/= 56;

	let d: char;

	d = 'a';

	d += 'a'; // FAIL

	let s: str;

	s = "as";

	s += "df"; 

	s -= "ef"; // FAIL
}